%% Siege Perilous MMO

{application, sp, [
	{description, "Siege Perilous MMO."},
	{vsn, "1"},
	{modules, []},
	{registered, [sp_sup]},
	{applications, [
        lager,
		kernel,
        crypto,
		stdlib,
        mnesia,
        compiler,
        syntax_tools,
		cowboy,
        jsx,
        bson,
        parsexml,
        pqueue
	]},
	{mod, {sp_app, []}},
	{env, []}
]}.
