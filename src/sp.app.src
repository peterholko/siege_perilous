%% Siege Perilous MMO

{application, sp, [
	{description, "Siege Perilous MMO."},
	{vsn, "1"},
	{modules, []},
	{registered, [sp_sup]},
	{applications, [
        lager,
		kernel,
        crypto,
		stdlib,
        compiler,
        syntax_tools,
		cowboy,
        jiffy
	]},
	{mod, {sp_app, []}},
	{env, []}
]}.
